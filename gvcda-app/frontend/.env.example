# Copy to .env.production and set before running `npm run build` for a real deploy.
# Leave unset for local dev — Vite's dev server proxies /api to localhost:4000 (see vite.config.js).
VITE_API_URL=https://gvcda-backend.onrender.com
